sudo apt update
sudo apt install ffmpeg
pip install pydub

#In MacOSX you would need to execute the following command to install ffmpeg
#brew install ffmpeg --with-libvorbis --with-sdl2 --with-theora
